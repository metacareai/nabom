rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 익명 인증(auth.uid) 소유자만 자신의 문서를 읽고 쓸 수 있음
    match /users/{userId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;

      match /guides/{guideId} {
        allow read, write: if request.auth != null && request.auth.uid == userId;
      }
    }
  }
}
